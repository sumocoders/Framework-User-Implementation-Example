{% extends '@SumoCodersFrameworkCore/base.html.twig' %}

{% block main %}
    {% if is_granted('IS_IMPERSONATOR') %}
        <a href="{{ impersonation_exit_path(path('user_overview') ) }}">{{ 'Exit impersonation'|trans }}</a>
    {% endif %}
{% endblock %}
